void main() {
  int x = 20, y = 12;
  var operation = '-';
  switch (operation) {
    case '+':
      print(x + y);
      return;

    case '-':
      print(x - y);
      return;

    case '*':
      print(x * y);
      return;
      
    case '/':
      print(x / y);
      return;
  }
}
